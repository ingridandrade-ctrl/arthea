{
  "installCommand": "npm ci && npx prisma generate",
  "buildCommand": "npx prisma generate && next build",
  "functions": {
    "src/app/api/**/*.ts": {
      "memory": 1024,
      "maxDuration": 30
    }
  }
}
