{
  "installCommand": "npm ci && npx prisma generate",
  "buildCommand": "npm run vercel-build",
  "regions": ["gru1"],
  "functions": {
    "src/app/api/**/*.ts": {
      "memory": 1024,
      "maxDuration": 60
    }
  }
}
