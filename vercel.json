{
  "installCommand": "npm ci && npx prisma generate",
  "buildCommand": "npx prisma generate && npx prisma db execute --file prisma/migrations-manual/2026-06-27_lead_status_string.sql --schema prisma/schema.prisma && npx prisma db push --skip-generate --accept-data-loss && next build",
  "regions": ["gru1"],
  "functions": {
    "src/app/api/**/*.ts": {
      "memory": 1024,
      "maxDuration": 60
    }
  }
}
